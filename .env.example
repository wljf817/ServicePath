SERVICEPATH_DEBUG=
SERVICEPATH_CONFIG_FILE=./servicepath.config.example.json

# Docker publishes to loopback by default. Change this only when intentional.
SERVICEPATH_BIND_ADDRESS=127.0.0.1
SERVICEPATH_PORT=5050
